package com.ctw.workstation.tests;

public class HelloAcademy {
    public String sayHello(String name) {
        if (name != null)
            return "Hello " + name;
        else
            return "Hello";
    }
}
